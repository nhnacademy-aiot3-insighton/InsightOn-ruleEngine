name: rule-engine CI/CD

on:
  push:
    branches: [ main, dev-deploy, dev ]
  pull_request:
    branches: [ main, dev-deploy, dev ]

jobs:
  call-reusable:
    uses: nhnacademy-aiot3-insighton/InsightOn-infra/.github/workflows/gateway-ci-cd.yml@archive/before-migration-k8s
    permissions:
      contents: read
      packages: write
    secrets: inherit